//Aula: Operações matematicas
        var y = 5; //declarar variaveis
        var a = 2, b = 15; //declarar variaveis uma após a outra:
        var soma = y + a + b;
        console.log(soma);
        var modulo = y % a;//modulo, resto de uma divisão
        console.log(modulo);
        y+=b;//incrementar pode alterar outras variaveis ou outras operações (y=y+b)
        console.log(y);
    
